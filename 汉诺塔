#include <stdio.h>

int hanoi1(int n)
{
	if (n > 1)
	{
		return 2 * hanoi1(n - 1) + 1;
	}
	if (n = 1)
	{
		return 1;
	}
}

int main()
{
	int total = 0;
	int n = 0;
	scanf_s("%d", &n);
	total = hanoi1(n);
	printf("%d", total);
}
