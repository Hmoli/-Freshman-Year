#include <stdio.h>
int main()
{
	int high[10] = { 0 };
	int height = 0;
	int i = 0;
	int quanlity = 0;
	for (; i < 10; i++)
	{
		scanf_s("%d", &high[i]);
	}
	scanf_s("%d", &height);
	height += 30;
	for (i = 0; i < 10; i++)
	{
		if (high[i] <= height)quanlity++;
	}
	printf("%d", quanlity);
